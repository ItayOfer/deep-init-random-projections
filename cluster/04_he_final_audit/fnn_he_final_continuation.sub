#!/bin/bash
#SBATCH --job-name=fnn_he_final_cont
#SBATCH -p dlc
#SBATCH --gres=gpu:1
#SBATCH --exclude=dgx04
#SBATCH --cpus-per-task=8
#SBATCH --mem=64G
#SBATCH --time=03:00:00
#SBATCH --output=%x-%j.out

set -euo pipefail

CONTAINER_IMAGE="${HOME}/nvidia_pt.sqsh"
CODE_DIR="${HOME}/thesis"

srun \
  --container-image="${CONTAINER_IMAGE}" \
  --container-mounts="${CODE_DIR}:/mount" \
  --container-workdir=/mount \
  bash -lc "python -u cluster/04_he_final_audit/run_final.py \
    --epochs 200 \
    --diagnostics-every 10 \
    --seed 42 \
    --device cuda \
    --checkpoint-dir reports/checkpoints/final_audit_continuation \
    --checkpoint-every 50 \
    --experiments 11_fmnist_100L_nobn,12_fmnist_100L_bn \
    --output reports/results/final_audit_continuation.json"
