# Cluster connection settings — copy to cluster/cluster.env and fill in.
# cluster.env is gitignored; it never leaves your machine.
#
#   cp cluster/cluster.env.example cluster/cluster.env
#
CLUSTER_USER="your-username"
CLUSTER_HOST="your.cluster.hostname"
REMOTE_DIR="~/thesis"
